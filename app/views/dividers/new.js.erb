$('.modal-title').html("<%= t('training.category.action.form.add_divider.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, divider: @divider }) %>");
$('#myModal').modal('show');
