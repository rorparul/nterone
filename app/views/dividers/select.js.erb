$('.modal-title').html('Edit Divider');
$('.modal-body').html("<%= escape_javascript(render partial: 'select', locals: {platform: @platform, divider: @divider, dividers: @dividers}) %>");
$('.modal-body').append("<hr>");
$('.modal-body').append("<%= escape_javascript(render partial: 'form', locals: {platform: @platform, divider: @divider}) %>");
// $('.modal-body .form-container select, .modal-body .form-container input').attr('id', 'disabledInput');
// $('.modal-body .form-container select, .modal-body .form-container input').attr('disabled', 'true');
$('#myModal').modal('show');
