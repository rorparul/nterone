$('.modal-title').html("<%= t('training.category.action.form.edit_certification.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'select', locals: {platform: @platform, subjects: @subjects}) %>");
$('.modal-body').append("<hr>");
$('.modal-body').append("<%= escape_javascript(render partial: 'form', locals: {platform: @platform, categories: @categories, subject: @subject}) %>");
$('#myModal').modal('show');
