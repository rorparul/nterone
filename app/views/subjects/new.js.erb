$('.modal-title').html("<%= t('subjects.modal.new.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: {platform: @platform, categories: @categories, subject: @subject}) %>");
$('#myModal').modal('show');
