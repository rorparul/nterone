$('.modal-title').html("<%= Time.now.strftime("The time is now %l:%M%p on %A, %B %d") %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'index', locals: {tasks: @tasks}) %>");
$('#myModal').modal('show');
