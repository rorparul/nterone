$('.modal-title').html("<%= t('custom_items.select_js..edit_custom_item') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'select', locals: {platform: @platform, custom_item: @custom_item, custom_items: @custom_items}) %>");
$('.modal-body').append("<hr>");
$('.modal-body').append("<%= escape_javascript(render partial: 'form', locals: {platform: @platform, custom_item: @custom_item}) %>");
$('#myModal').modal('show');
