$('.modal-title').html('Add Custom Item');
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, custom_item: @custom_item }) %>");
$('#myModal').modal('show');
