$('.modal-title').html("<%= t('modal.order_items.edit.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { order_item: @order_item }) %>");
$('#myModal').modal('show');
