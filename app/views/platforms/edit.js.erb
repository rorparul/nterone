$('.modal-title').html("<%= t('training.platform.form.edit_platform.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform }) %>");
$('#myModal').modal('show');
