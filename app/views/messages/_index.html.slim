.panel.panel-default
  .panel-heading
    strong.text-muted = "#{pluralize(messages.count, t('admin.admin_messages.row.title'))}"
  .table-responsive
    table.table.table-condensed
      thead
        tr
          th
            = t 'admin.admin_messages.row.column.date'
          th 
            = t 'admin.admin_messages.row.column.from'
          th
            = t 'admin.admin_messages.row.column.message'
      tbody
        - messages.each do |message|
          tr
            td = message.announcement.created_at.strftime("%B %d, %Y")
            td
            td = message.announcement.content
