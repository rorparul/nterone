$('.modal-title').html("<%= t('training.category.action.form.add_exam.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, exam: @exam }) %>");
$('#myModal').modal('show');
