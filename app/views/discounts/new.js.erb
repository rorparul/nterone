$('.modal-title').html("<%= t('discounts.new_js.heading') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { discount: @discount }) %>");
$('#myModal').modal('show');
