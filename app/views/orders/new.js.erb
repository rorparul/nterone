$('#myModal .modal-title').html("<%= t('orders.new_js.add_order') %>");
$('#myModal .modal-body').html("<%= escape_javascript(render partial: 'form', locals: { order: @order, user: @user, video_on_demands: @video_on_demands }) %>");
$('#myModal').modal('show');
