$('.modal-title').html("Add Purchase for <%= @user.full_name %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { order: @order, user: @user, video_on_demands: @video_on_demands }) %>");
$('#myModal').modal('show');
