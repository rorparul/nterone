$('.modal-title').html("<%= t('orders.new_admin_js.add_order') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form_for_admin', locals: { order: @order, user: @user, errors: true }) %>");
$('#myModal').modal('show');
