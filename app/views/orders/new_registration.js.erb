$('.modal-title').html("Add Registration");
$('.modal-body').html("<%= escape_javascript(render partial: 'form_for_registration', locals: { order: @order, event: @event }) %>");
$('#myModal').modal('show');
