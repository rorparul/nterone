$('.modal-title').html('Edit Category');
$('.modal-body').html("<%= escape_javascript(render partial: 'select', locals: { forums: @forums }) %>");
$('.modal-body').append("<hr>");
$('.modal-body').append("<%= escape_javascript(render partial: 'form', locals: { forum: @forum }) %>");
$('#myModal').modal('show');
