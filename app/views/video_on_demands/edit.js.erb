$('.modal-title').html("<%= t('video_on_demands.modal.edit.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, video_on_demand: @video_on_demand, categories: @categories, instructors: @instructors }) %>");
$('#myModal').modal('show');
