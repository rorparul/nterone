$('.modal-title').html("<%= t('training.category.action.form.add_category.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, category: @category }) %>");
$('#myModal').modal('show');
