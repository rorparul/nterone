wb = xlsx_package.workbook

wb.styles do |style|
  color = style.add_style(bg_color: '9CBA5F')

  wb.add_worksheet(name: 'All') do |sheet|
    headers = [
      t('models.platform.singular'),
      t('models.course.abbreviation'),
      t('models.course.title'),
      t('models.course.slug'),
      t('views.general.url')
    ]

    sheet.add_row headers, style: Array.new(headers.size, color)

    @courses.each do |course|
      sheet.add_row [
        course.platform.title,
        course.abbreviation,
        course.title,
        course.slug,
        platform_course_url(course.platform, course)
      ]
    end
  end
end
