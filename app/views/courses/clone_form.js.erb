$('.modal-title').html("<%= t('courses.clone_form.clone_course') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'clone_form', locals: { course: @course }) %>");
$('#myModal').modal('show');
