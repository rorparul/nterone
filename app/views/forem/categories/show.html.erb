<%- content_for :breadcrumbs do %>
  <a href="<%= main_app.root_path %>">
    <span class="fa fa-home"></span>
    <%= t('breadcrumbs.home') %>
  </a>
  <span class="seperator">/</span>
  <%= link_to t('forem.forum.forums'), forem.forums_path %>
  <span class="seperator">/</span>
  <span class="text-muted"><%= forem_emojify(@category) %></span>
<% end %>

<div class='category'>
  <!-- <h2><%= link_to t('forem.forum.forums'), forem.forums_path %> &raquo; <%= link_to forem_emojify(@category), [forem, @category] %></h2> -->
  <%= render @category %>
</div>

<script>
  $('.forums-tab').addClass('active');
</script>
