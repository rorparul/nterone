<%- content_for :breadcrumbs do %>
  <a href="<%= main_app.root_path %>">
    <span class="fa fa-home"></span>
    <%= t('admins.partial_breadcrumbs.home') %>
  </a>
  <span class="seperator">/</span>
  <span class="text-muted"><%= t('admins.partial_breadcrumbs.forums') %></span>
<% end %>

<h2><%= t("forem.admin.categories.edit.edit", :title => @category.name) %></h2>

<%= render "form" %>
