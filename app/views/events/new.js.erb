$('.modal-title').html("<%= t('events.new.add_event') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, event: @event, course: @course }) %>");
$('#myModal').modal('show');
