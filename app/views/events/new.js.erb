$('.modal-title').html("<%= t('modal.events.new.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, event: @event, course: @course, instructors: @instructors }) %>");
$('#myModal').modal('show');
