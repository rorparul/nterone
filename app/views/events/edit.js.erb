$('.modal-title').html("<%= t('admin.admin_classes.form.edit_event.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, course: @course, event: @event, instructors: @instructors }) %>");
$('#myModal').modal('show');
