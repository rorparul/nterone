.container
  .row
    .col-xs-12.col-md-8.col-md-offset-2 style='margin-top: 40px;'
      - @enrollments.reverse.each do |enrollment|
        - enrollment.each do |k, v|
          p
            strong = "#{k}: "
            = v

        hr
