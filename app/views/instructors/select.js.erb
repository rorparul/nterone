$('.modal-title').html("<%= t('training.category.action.form.edit_instructor.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'select', locals: {platform: @platform, instructor: @instructor, instructors: @instructors}) %>");
$('.modal-body').append("<hr>");
$('.modal-body').append("<%= escape_javascript(render partial: 'form', locals: {platform: @platform, instructor: @instructor}) %>");
$('#myModal').modal('show');
