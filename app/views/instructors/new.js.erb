$('.modal-title').html('Add Instructor');
$('.modal-body').html("<%= escape_javascript(render partial: 'form', locals: { platform: @platform, instructor: @instructor }) %>");
$('#myModal').modal('show');
