$('.modal-title').html("<%= t('modal.instructors.show.title') %>");
$('.modal-body').html("<%= escape_javascript(render partial: 'show', locals: { instructor: @instructor }) %>");
$('#myModal').modal('show');
